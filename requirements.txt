# Core web/API dependencies
fastapi>=0.110,<1
httpx>=0.27,<1
numpy>=1.24,<3
pydantic>=2.6,<3
uvicorn[standard]>=0.29,<1

# vLLM serving backend. Linux (x86_64/aarch64) with a CUDA-capable device.
vllm>=0.6,<1

# WebSocket client for the separately released Confucius4-R2T2 ASR service.
websockets>=12,<16

# Local Hugging Face translation backend (TRANSLATION_BACKEND=hf). Not needed
# when serving through vLLM. ASR does not use these dependencies: it runs in
# the separately deployed R2T2 service.
accelerate>=0.30,<2
huggingface-hub>=0.24,<2
safetensors>=0.4,<1
torch>=2.1
transformers>=4.45,<6
